{
  "name": "@butterfly/server-app",
  "version": "0.1.0",
  "description": "Butterfly Agent — HTTP server executable (thin entry point over @butterfly/server)",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@butterfly/server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4",
    "typescript": "~5.7.3"
  }
}
