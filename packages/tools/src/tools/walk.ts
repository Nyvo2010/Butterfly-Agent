// Re-export the canonical walk from @butterfly/core (single source of truth).
import { SKIP_DIRS } from "@butterfly/core"

export { walk } from "@butterfly/core"
export { SKIP_DIRS as DEFAULT_SKIP_DIRS }
