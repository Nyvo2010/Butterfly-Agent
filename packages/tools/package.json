{
  "name": "@butterfly/tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "echo 'Use pnpm test from project root to run all tests'"
  },
  "files": ["dist"],
  "dependencies": {
    "@butterfly/core": "workspace:*",
    "picomatch": "^4.0.0"
  },
  "devDependencies": {
    "@types/picomatch": "^3.0.0"
  }
}
