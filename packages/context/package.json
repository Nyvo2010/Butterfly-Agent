{
  "name": "@butterfly/context",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "echo 'Use pnpm test from project root to run all tests'"
  },
  "files": ["dist"],
  "dependencies": {
    "@butterfly/core": "workspace:*",
    "@butterfly/session": "workspace:*",
    "gpt-tokenizer": "^3.0.0"
  }
}
